# Optional — the demo runs on the shared read-only project by default.
# Drop in your own credentials to watch discovery register and translate
# your phrases live (write key = registration; read-only = fetch only).
#LANGSYS_PROJECT_ID=
#LANGSYS_API_KEY=
# Point at a non-production API instance (local dev):
#LANGSYS_API_URL=
#PORT=3000
